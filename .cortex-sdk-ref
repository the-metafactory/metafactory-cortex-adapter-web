# Pinned cortex ref the surface-sdk .d.ts is synced from (cortex#1950).
# Bump to adopt a new SURFACE_SDK_VERSION; see scripts/sync-surface-sdk.ts.
# Pinned to the #1956 squash-merge commit on cortex main (durable — the
# pre-merge branch SHA was GC-doomed, #1956 review). Repin to a cortex
# release tag (e.g. v6.8.0) once one is cut.
7f9953fa
